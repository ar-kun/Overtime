using API.Models;

namespace API.Contracts
{
    // Defines an interface IAccountRepository that inherits from the IGeneralRepository<Account> interface
    public interface IAccountRepository : IGeneralRepository<Account>
    {
    }
}
